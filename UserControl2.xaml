<UserControl x:Class="mems_fx3lp.UserControl2"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:oxy="clr-namespace:OxyPlot.Wpf;assembly=OxyPlot.Wpf"
             xmlns:local="clr-namespace:mems_fx3lp"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800" MinWidth="800" MinHeight="600" >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200*" MinWidth="200"/>
            <ColumnDefinition Width="100*" MinWidth="100"/>
        </Grid.ColumnDefinitions>
        <oxy:PlotView Model="{Binding ScatterModel3}" Margin="0,0,121,392"  Grid.ColumnSpan="2"/>
        <oxy:PlotView Model="{Binding ScatterModel4}" Margin="0,208,121,197" Grid.ColumnSpan="2"/>
        <oxy:PlotView Model="{Binding ScatterModel5}" Margin="0,403,121,0" Grid.ColumnSpan="2"/>
        <!--<Button x:Name="button" Grid.Column="1" Content="Button" HorizontalAlignment="Left" Margin="0,400,0,0" VerticalAlignment="Top" Width="75"/>-->
    </Grid>
</UserControl>
